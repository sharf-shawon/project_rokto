# CLAUDE.md

READ AGENTS.md FIRST. This file contains Claude-specific notes only.

## Guidance

- **Source of Truth**: Defer all architectural and workflow decisions to `AGENTS.md`.
- **Workflow**: Strictly follow the OpenSpec lifecycle (`/opsx:*`) as outlined in `AGENTS.md`.
- **Verification**: Ensure `just check` passes before concluding any task.
- **Environment**: Use `just` for development tasks and `uv` for Python dependency management.
